# =============================================================================
# EXAMPLE sample sheet for the STAR + RSeQC + TEtranscripts workflow.
#
# To run an analysis, copy this to config/samples.csv and fill in your rows:
#
#   cp config/samples.example.csv config/samples.csv
#
# config/samples.csv is gitignored (it belongs to your analysis, not the
# repo). Rows starting with '#' are ignored.
#
# Columns (order matters):
#   sample        unique sample name (no spaces)
#   fastq_1       read 1 / single-end fastq(.gz); absolute path or relative
#                 to the repo root (where you run snakemake from)
#   fastq_2       read 2 fastq(.gz); leave empty for single-end. Paired and
#                 single-end samples can be mixed.
#   strandedness  per-sample override: auto, forward, reverse, unstranded,
#                 or TEtranscripts' "no". Blank = auto (RSeQC detection).
#   condition     biological group label. Include it to run TEtranscripts
#                 differential analysis (one contrast per pair of distinct
#                 groups); omit/blank it to only quantify counts.
# =============================================================================
sample,fastq_1,fastq_2,strandedness,condition
sampleA_rep1,reads/sampleA_rep1_R1.fastq.gz,reads/sampleA_rep1_R2.fastq.gz,auto,treatment
sampleA_rep2,reads/sampleA_rep2_R1.fastq.gz,reads/sampleA_rep2_R2.fastq.gz,auto,treatment
sampleB_rep1,reads/sampleB_rep1_R1.fastq.gz,reads/sampleB_rep1_R2.fastq.gz,auto,control
sampleB_rep2,reads/sampleB_rep2_R1.fastq.gz,reads/sampleB_rep2_R2.fastq.gz,auto,control
