$schema: "http://json-schema.org/draft-07/schema#"
description: row of config/samples.csv
type: object

properties:
  sample:
    type: string
    description: unique sample identifier, used throughout the workflow
  fastq_1:
    type: string
    description: path to read 1 (or single-end) fastq(.gz) file
  fastq_2:
    type: [string, "null"]
    description: >-
      path to read 2 fastq(.gz) file. Optional per-sample: leave empty for a
      single-end sample. A sample sheet can freely mix paired-end and
      single-end samples.
  strandedness:
    type: [string, "null"]
    description: >-
      Optional per-sample library strandedness override, using nf-core/
      rnaseq's vocabulary so an nf-core samplesheet can be used directly:
      "auto" (RSeQC auto-detection), "forward", "reverse", or "unstranded"
      (TEtranscripts' "no" is also accepted). Leave empty to fall back to
      strandedness.mode in config.yaml for that sample.
  condition:
    type: [string, "null"]
    description: >-
      Optional. Biological condition/group label. If this column is present
      (and filled in for every sample), the workflow automatically runs a
      TEtranscripts differential analysis for every pairwise combination of
      distinct condition values. If absent, differential analysis is skipped.

required:
  - sample
  - fastq_1
