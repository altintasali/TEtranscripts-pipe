# TO-DO List

- `tetranscripts-pipe` script (`init` and `samples`) should be more flexible for creating the samplesheet.csv. It should be written to current directory
- make all "write_igv_to_bed" rules "true"
