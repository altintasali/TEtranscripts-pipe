name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  dag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: conda-incubator/setup-miniconda@v3
        with:
          activate-environment: rnaseq-star-tetranscripts
          environment-file: environment.yaml
          channel-priority: strict
      - run: snakemake --configfile config/test.yaml --sdm conda --cores 2 -n

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: conda-incubator/setup-miniconda@v3
        with:
          activate-environment: rnaseq-star-tetranscripts
          environment-file: environment.yaml
          channel-priority: strict
      - run: snakemake --configfile config/test.yaml --sdm conda --cores 2
